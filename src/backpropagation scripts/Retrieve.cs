using UnityEngine;
using System.Collections;

public class Retrieve : MonoBehaviour {

//	public double[] _weights;
	public bool retrieve;
	 int k = 0;
	// Use this for initialization
	void Start () {
		retrieve = false;
//		_weights = new double[2451];
	}
	
	// Update is called once per frame
	void Update () {
//		if (retrieve == false){
//			for (int i = 0; i < 2451; i++) {
//				_weights[i] = double.Parse( PlayerPrefs.GetString("FinalWeights" + i));
//				
//			}
//
//			retrieve = true;
//		}
//	
	}
}
